- Evite fazer longos trechos repetidos de código nos testes, pegue só pequenas partes
- Fazer um botão de voltar no StoryViewer


import React from "react";
import Stories from "react-insta-stories";

const StoryViewer = ({ storiesList, currentStory, setCurrentStory }) => {
  const story = storiesList.filter((story) => story.id === currentStory);

  return (
    <div className="container-fluid d-flex justify-content-center min-100-vh bg-dark">
      <Stories
        key={story.id}
        storyStyles={{ width: "100vw" }}
        stories={story.storyInfo}
        defaultInterval={4000}
        onAllStoriesEnd={() => {
          if (currentStory == storiesList.length - 1) {
            setCurrentStory(0);
          } else {
            setCurrentStory((actual) => actual + 1);
          }
        }}
        width={320}
        height={"100vh"}
      />
    </div>
  );
};

export default StoryViewer;
